{
  "name": "app-tetris",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve dist -s -n -L -p $PORT"
  },
  "devDependencies": {
    "standard": "^17.1.0",
    "vite": "^5.0.8"
  },
  "eslintConfig": {
    "extends": [
      "standard"
    ]
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
